{
  "name": "hello-world",
  "version": "0.1.0",
  "main": "main.js",
  "dependencies": {
    "electron-prebuilt": "^0.36.8"
  },
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . HelloWorld --platform=all --arch=x64 --version 0.36.8 --overwrite --out ./build"
  },
  "devDependencies": {
    "electron-packager": "^5.2.1"
  }
}
